<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.diealbalb">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_logorojo_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_logorojo_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".diseño.ResetPassword"
            android:theme="@style/NoActionThem" />

        <activity android:name=".diseño.SingIn"
            android:theme="@style/NoActionThem"/>

        <activity android:name=".diseño.Login" />

        <activity android:name=".diseño.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity"
            android:theme="@style/NoActionThem"/>

    </application>

</manifest>